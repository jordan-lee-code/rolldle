// Ko-fi support details in one place. We render our own button rather than Ko-fi's
// overlay widget, whose default white button doesn't sit well against the dark theme,
// so the support links carry the game's own palette instead.
export const KOFI_USERNAME = 'JourneyJordan';
export const KOFI_URL = `https://ko-fi.com/${KOFI_USERNAME}`;
